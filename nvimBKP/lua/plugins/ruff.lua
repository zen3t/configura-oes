return {
    "astral-sh/ruff-lsp",
    config = function()
        -- ruff_lsp DEPRECADO → agora é só "ruff"
        -- E NÃO tem `.setup`
        -- O LSP é configurado no próprio lsp.lua
    end,
}

